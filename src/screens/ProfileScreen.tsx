import {Screen} from '../components/Screen';
import {Title} from '../components/Title';

export const ProfileScreen = () => {
  return (
    <Screen>
      <Title>Profile</Title>
    </Screen>
  );
};
